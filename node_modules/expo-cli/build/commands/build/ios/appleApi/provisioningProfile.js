"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _fastlane() {
  const data = require("./fastlane");

  _fastlane = function () {
    return data;
  };

  return data;
}

const createManager = ({
  appleId,
  appleIdPassword,
  team,
  bundleIdentifier
}) => ({
  async create(metadata) {
    const args = ['create', appleId, appleIdPassword, team.id, team.inHouse, bundleIdentifier, metadata.distCertSerialNumber || '__last__'];
    return await (0, _fastlane().runAction)(_fastlane().travelingFastlane.manageProvisioningProfiles, args);
  },

  async revoke() {
    const args = ['revoke', appleId, appleIdPassword, team.id, team.inHouse, bundleIdentifier];
    await (0, _fastlane().runAction)(_fastlane().travelingFastlane.manageProvisioningProfiles, args);
  }

});

var _default = createManager;
exports.default = _default;
//# sourceMappingURL=../../../../__sourcemaps__/commands/build/ios/appleApi/provisioningProfile.js.map
